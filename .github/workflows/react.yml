name: Build automático

on:
  push:
    branches: [develop, master]
  pull_request:
    branches: [ develop, master ]

jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: 18
          - run: npm ci
          - run: npm run build
      
      compilar:
        runs-on: ubuntu-latest
        steps:
          - run: echo "🔧 Compilando app..." # <-- Esta línea fue corregida
    
      test:
        runs-on: ubuntu-latest
        needs: compilar
        steps:
          - run: echo "🧪 Ejecutando pruebas..." # <-- Esta línea fue corregida
    
      deploy:
        runs-on: ubuntu-latest
        needs: [test]
        steps:
          - run: echo "🚀 Deploy listo!" # <-- Esta línea fue corregida